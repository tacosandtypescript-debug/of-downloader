{
  "name": "of-backup-project-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "test:extension": "node --test tests/test_extension.mjs",
    "prepare:chrome": "node scripts/prepare-chrome.mjs",
    "build:chrome": "npm run prepare:chrome && npx --yes web-ext build --source-dir build/chrome --artifacts-dir artifacts --filename of_downloader_exporter-chrome-1.0.2.zip --overwrite-dest"
  }
}
