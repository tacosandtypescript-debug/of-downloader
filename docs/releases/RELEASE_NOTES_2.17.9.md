# OF Downloader 2.17.9

- En Termux, Linux y Windows la descarga avisa con `INICIANDO DESCARGA` y `Motor lanzado`.
- Confirmar un perfil con Enter, `s`, `si` o `sí` ya arranca la descarga (antes Enter cancelaba).
- La detección previa aclara que todavía no es la descarga.
- El actualizador de Termux copia `deploy/termux/launcher` y `requirements/termux.txt`.
